export type Author = {
    id: string,
    name: string,
    bio: string,
    logo: string,
    rating: number,
    // genres?: Array<Genre>,
    // albums?: Array<AlbumList>,
    // compositions?: Array<Track>
}
